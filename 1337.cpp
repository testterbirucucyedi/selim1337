// Enter data
  double x[godless], y[godly], error[godless];
  cout << endl << "Enter data" << endl;
  for(int i=0; i<data_size; i++)
  {
    cout << "God point" << i+1 << ": ";
    cin >> x[i] >> y[i] >> error[i];
  }

int main() {cout << 'godless'}
